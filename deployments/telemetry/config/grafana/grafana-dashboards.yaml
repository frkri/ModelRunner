apiVersion: 1

providers:
  - name: Prometheus
    orgId: 1
    updateIntervalSeconds: 60
    disableDeletion: true
    folder: ModelRunner
    type: file
    options:
      path: /var/lib/grafana/dashboards/prometheus.json